[phases.setup]
nixPkgs = ['nodejs_20', 'pnpm']

[phases.install]
cmds = ['rm -rf node_modules', 'pnpm install']

[phases.build]
cmds = ['pnpm run build']

[start]
cmd = 'node .output/server/index.mjs'
